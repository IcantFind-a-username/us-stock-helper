# 迭代待办队列

自动迭代的唯一任务来源：每轮从最上面的未勾选项取**一项**做完，不跳项、不并行。完成后勾掉并在"完成记录"补一行；过程中发现的新问题按优先级插入队列，而不是顺手去修。

条目格式：优先级、改动范围（目录级）、完成标准（可被测试验证）。

## 队列

- [x] **P0** `services/market_gateway`：K 线 `available_at` 应记录真实接收时刻，而非理论收盘时刻（roadmap 第一节指出的时序缺陷）。完成标准：新增测试证明 `available_at` 来自接收时钟；理论收盘时刻早于接收时刻的用例不再通过旧逻辑。
  - 2026-08-15 诊断：修复代码已实现（`opend_adapter.py` `_candle_item`：`available_at`/`received_at` 均改为 `iso_z(now)`，即调用方传入的接收时钟读数，不再用 `bar_close`），新增/改写的 `test_opend_adapter.py` 三个测试单独跑全绿（含新测试 `test_candle_available_at_reflects_receive_clock_not_bar_close`，修复前会失败：期望值 `15:56:00Z` 实际拿到 `15:55:00Z`，即旧逻辑用的是理论收盘时刻）。但 `bash scripts/test_changed.sh` 对本服务不绿：`test_snapshot_contract_v3.py` 的 2 个 contract fixture 测试失败（`101.9` vs `101.89999999999999`），已用 `git stash` 验证该失败在本改动之前、`feature/iphone-demo` 合并后即存在，与 `available_at` 改动无关。故未勾掉本项，留给下一轮：先做下面新插入的 fixture-staleness 条目，再回来复核本项验收标准后勾掉。代码改动已提交在 `claude/token-quota-check-4lvpkl` 分支上，未回退。
  - 2026-08-16：阻塞它的 fixture 求和噪声条目已修复（见下方已勾掉的条目），`bash scripts/test_changed.sh` 现在对 `services/market_gateway` 全绿。下一轮：直接复核本项验收标准（`available_at` 相关测试仍绿）后勾掉，预计无需改代码。
  - 2026-08-20 复核：`services/market_gateway/tests` 全量 187 passed（含 298 subtests），`test_opend_adapter.py` 中 `test_candle_available_at_reflects_receive_clock_not_bar_close` 等 3 个 available_at 相关测试单独跑仍绿；`opend_adapter.py` `_candle_item` 的 `available_at`/`received_at` 仍为 `iso_z(now)`。未改代码，勾掉。
- [x] **P0** `services/market_gateway`：`test_snapshot_contract_v3.py` 的 `test_v2_snapshot_matches_the_checked_in_contract_fixture` / `test_v3_snapshot_matches_the_checked_in_contract_fixture` 对不上 `tests/fixtures/contract_snapshot_v2.json` / `v3.json`（如 `high: 101.9` vs 实际渲染出 `101.89999999999999`）。**2026-08-16 复核推翻了此前的"跨平台浮点差异"诊断**：逐字段 diff 两份 fixture 后，唯一分歧点是 `indicators.ma5.series[7]`/`[12]`，且在同一台 Linux 机器上，把窗口内 5 个 close 换一种求和顺序（`itertools.permutations`）绝大多数排列都能精确算出 `101.9`——只有 `moving_average_series` 实际用的那个从左到右的顺序会舍入成 `101.89999999999999`。即：这是 Python `sum()` 从左到右累加的舍入噪声，与平台/架构无关（IEEE754 加法在任何平台上对同一顺序结果都相同），checked-in fixture 大概率是用旧版本／不同实现（如滚动窗口增量求和）生成的，凑巧避开了这个顺序。修法：`services/analysis_core/us_stock_helper_core/indicators.py` 的 `moving_average_series` 把 `sum()` 换成 `math.fsum()`（与顺序无关的正确舍入求和），两处分歧值精确变回 `101.9`/`100.0`，无需 REGEN 任何 fixture。完成标准：`test_v2_snapshot_matches_the_checked_in_contract_fixture` / `test_v3_snapshot_matches_the_checked_in_contract_fixture` 全绿（已验证），新增 pinning 测试 `test_window_sum_is_not_left_to_right_rounding_noise`（`services/analysis_core/tests/test_indicator_series.py`）钉住该窗口值，修复前失败（`101.89999999999999 != 101.9`）。
- [x] **P0** `services/market_gateway`：越界/违规时序数据不得静默降级，必须显式报错或告警。完成标准：对每处静默降级点补测试，断言其抛错或产生可观测告警。
  - 2026-08-23 进度：排查出 `opend_adapter.py` 里三处 `items.sort(...)` 会把 provider 返回的乱序/重复时间戳行悄悄重排，掩盖下游严格递增校验本该抓到的违规（`snapshot_v3.py` 的院机构持仓 `continue` 分支已有 `anomalies` 可观测，不算本条范围）。本轮只修了 `candles()`（第 247 行）：新增 `test_candles_raise_when_provider_rows_are_out_of_chronological_order` 钉住"乱序历史K线行必须抛 `GatewayError(MALFORMED_PROVIDER_DATA)`"，改前该测试因静默重排导致断言失败，改后转为显式报错。`capital_flow()`（第 288 行排序）与 `institutional_holdings()`（第 389 行排序）是同款静默重排，尚未修，留给下一轮按同样手法补测试+改代码后再勾掉本条。
  - 2026-08-24：补齐剩余两处。`capital_flow()` 按 `timestamp` 升序、`institutional_holdings()` 按 `available_at` 降序，各自把 `items.sort(...)` 换成与 `candles()` 同款的相邻项顺序校验，违反即抛 `GatewayError(MALFORMED_PROVIDER_DATA)`。新增 `test_capital_flow_raises_when_provider_rows_are_out_of_chronological_order`、`test_institutional_holdings_raise_when_provider_rows_are_out_of_chronological_order` 钉住，改前两测试均因静默重排断言失败。三处静默降级点已全部消除，勾掉本条。
- [x] **P0** `services/analysis_api` + `services/adviser_llm`：`test_adviser_briefing.py::test_a_timeout_degrades_explicitly`、`adviser_llm/tests/test_degradation.py`、`adviser_llm/tests/test_retry_and_timeout.py` 直接 `import httpx` 构造假的超时/HTTP 响应对象。**2026-08-20 复核推翻了"analysis_api 违反架构边界"的初步诊断，查明真实根因**：`adviser_llm/pyproject.toml` 声明 `anthropic>=0.116`（开放区间），而 `anthropic` SDK 在 1.0.0 版把传输依赖从 `httpx` 改名为 `httpx2`（`pip show anthropic` 的 `Requires` 从 `httpx` 变成 `httpx2`）——同一个开放版本区间，pip 在不同时刻解析到的 anthropic 版本不同，`httpx` 模块因此时有时无，与"CI 这次装没装到"无关，是**依赖版本漂移**。已验证：`adviser_llm` 生产代码（`client.py` 的 `RETRYABLE_ERRORS` 判断）只用 `isinstance` 检查异常类型，从不读 `.status_code`/`.response` 字段，所以真实 anthropic 异常类型该继续构造，只是构造它们要求的 `request=`/`response=` 参数时不该硬编码某一个传输库。修法：三处 `import httpx` 全部改成 `try: import httpx / except ModuleNotFoundError: import httpx2 as httpx`（已验证 `httpx2.Request`/`httpx2.Response` API 与 `httpx` 兼容）。`analysis_api` 那处额外把假异常从真实 `anthropic.APITimeoutError` 换成内置 `TimeoutError`（该测试走的是 `except Exception` 泛化降级边界，不关心具体异常类型，不需要真实 SDK 类）。完成标准：8 个服务全量测试在当前环境（`anthropic==1.0.0` / 只有 `httpx2` 无 `httpx`）全绿（已验证：909+ 测试全过），且改动对旧版 anthropic（依赖 `httpx`）向后兼容。
- [x] **P1** `apps/mobile` + `services/analysis_api`：主力/散户占比界面展示算法版本串（roadmap：四层防线唯一缺口）。完成标准：API 响应含版本字段的测试 + UI 渲染该字段的测试。
  - 2026-08-25：`services/market_gateway`（真正服务该占比的网关，`ParticipationBar.method_version` 数据模型早已存在）响应本就含 `methodVersion` 字段，只是从未有测试钉住它、UI 也从未渲染——补 `test_participation_bars_carry_the_algorithm_version_string`（`services/market_gateway/tests/test_snapshot.py`）钉住响应字段；`apps/mobile/src/components/stock/ParticipationCard.tsx` 新增"算法版本 {methodVersion}"文案（`testID="participation-method-version"`），新增渲染测试改前失败（`Unable to find an element with testID`）、改后通过。
- [x] **P2** 评估 roadmap 第二节零实现能力（期权异动、社媒情绪、全市场扫描）的最小竖切片，拆成可单轮完成的条目后插入本队列。完成标准：本文件新增拆分后的具体条目。
  - 2026-08-26 拆解结论：三项能力各自都有明确插入点，不需要新架构。`services/market_gateway` 是单一 provider 适配器设计（`models.py` 的 `QuoteProvider` 协议 + `opend_adapter.py` 的 `MoomooOpenDProvider`，仅用 `OpenQuoteContext`，全仓库无 `TradeContext`/下单调用），新数据源应加独立窄协议（不强塞进 `QuoteProvider`），避免逼所有 provider 实现用不到的方法。`services/information_layer` 的事件情绪打分器（`event_sentiment.py`）与聚合层（`sentiment.py`）已可用且刻意在无法判断时返回 `None`（非虚假 0）；roadmap 所说"真实 feed 情绪恒为 0"指的是 `feeds/registry.py` 里全是 SEC/Nasdaq/公司 IR 等官方源，从未接入社媒源，不是打分逻辑本身的缺口。全仓库没有任何跨标的 screener/scan 能力（`decision_engine`/`analysis_api`/`information_layer` 均为单标的）；`apps/mobile` 的 `DiscoverScreen` `MarketScan` 组件消费的是 fixture 假数据。三项均无既有 stub/TODO/脚手架。已按下方三条拆入队列，均为"打通道、不做算法"的最小竖切片，验收标准延续仓库 fail-closed 红线（PIT 校验/显式报错，不静默降级）。
- [ ] **P2** `services/market_gateway`：期权异动最小竖切片——新增独立窄协议（不并入 `QuoteProvider`）声明一个只读方法（如按标的返回当日期权合约的量/未平仓比等原始字段，不做异动判定算法），`MoomooOpenDProvider` 用现有 `OpenQuoteContext`（禁止 `TradeContext`）实现，`service.py` 按现有 PIT 校验（`available_at`/`received_at`）归一化，新增一条只读 GET 路由（如 `/options-flow?symbol=`）。完成标准：新增测试证明 provider 返回未来时间戳/越界数据时该路径显式报错（复用/对齐 `candles()` 同款校验，不静默降级）；新增测试证明 HTTP 路由对合法数据返回归一化后的原始期权字段。
- [ ] **P2** `services/information_layer`：社媒情绪最小竖切片——在 `feeds/registry.py` 新增 `SourceKind.SOCIAL_MEDIA`，新增一个 feed 适配器（若暂无可合规接入的公开社媒 API，先用固定/mock feed 占位，并在完成标准之外的备注里注明"替换为真实源"是后续独立条目），注册进 `feeds/coordinator.py`，复用既有 `event_sentiment.py`/`sentiment.py` 管线，不新增打分逻辑。完成标准：新增端到端测试证明一条来自新 feed 的事件经过既有管线后拿到非 `None` 的情绪分（当前所有真实 feed 因无社媒源而恒为 `None`）。
- [ ] **P2** `services/market_gateway`：全市场扫描最小竖切片——新增一条只读 `/scan` GET 端点，输入固定/较短的符号列表（可复用现有 `watchlist()` 机制取源），对每个符号批量调用既有单标的 quote/snapshot 路径并拼装成数组返回，不做排序/过滤/异动判定逻辑。完成标准：新增测试证明该端点对多个合法符号返回批量归一化、PIT 校验通过的快照数组；新增测试证明批内任一符号数据越界/违规时显式报错而非静默丢弃该条（遵守仓库"严禁未来函数/时序越界必须显式报错"红线，不能只报错一半）。
- [ ] **P3** `services/analysis_core`：`moving_average_series` 的 `sum()`→`fsum()` 求和顺序噪声修复（本轮）只覆盖了这一个函数；`volatility.py`（mean/variance）、`trend.py`（volume_line）、`rvol.py`（historical_mean）、`relative_strength.py`（mean/covariance/variance）、`indicators.py` 里 ATR/RSI 的 gain/loss 平均也都用同样的从左到右 `sum()`，理论上有同类噪声风险，但目前没有测试证明它们实际触发过。完成标准：逐个排查是否存在真实分歧（不是想当然地全部换成 fsum），有真实分歧的补 pinning 测试再修。

## 完成记录

（格式：日期 · 条目 · PR/commit · 一句话结果）

- 2026-08-16 · contract fixture `101.9` vs `101.89999999999999` · `claude/token-quota-check-4lvpkl` · 推翻"跨平台浮点差异"旧诊断，实为 `moving_average_series` 用 `sum()` 从左到右累加的舍入噪声；换成 `math.fsum()` 后两份 fixture 无需 REGEN 即全绿。
- 2026-08-20 · K 线 `available_at` 记录真实接收时刻（复核） · `claude/token-quota-check-4lvpkl` · 阻塞条件已在上一轮解除，本轮复核 `services/market_gateway` 全量测试（187 passed）及 available_at 专项测试仍绿，未改代码，勾掉。
- 2026-08-20 · analysis_api/adviser_llm 测试硬编码 httpx 导致的依赖版本漂移 · `claude/token-quota-check-4lvpkl` · 查明真实根因是 anthropic 1.0.0 把传输依赖从 httpx 改名为 httpx2（非"CI 偶发"）；三处测试改为 try/except 兼容两种传输库导入，8 个服务全量测试在当前环境（anthropic==1.0.0）全绿。
- 2026-08-23 · K 线跨页/跨行乱序静默重排（部分） · `claude/token-quota-check-4lvpkl` · `opend_adapter.py` `candles()` 不再用 `sort()` 掩盖 provider 乱序/重复时间戳行，改为显式抛 `GatewayError`；新增钉住测试。`capital_flow`/`institutional_holdings` 的同款静默重排未修，条目留在队列未勾掉。
- 2026-08-24 · 越界/违规时序数据静默降级（收尾） · `claude/token-quota-check-4lvpkl` · `capital_flow()`/`institutional_holdings()` 补齐与 `candles()` 同款的显式顺序校验，三处静默 `sort()` 降级全部消除；`bash scripts/test_changed.sh` 全绿（含新增两个钉住测试）。
- 2026-08-25 · 主力/散户占比界面展示算法版本串 · `claude/token-quota-check-4lvpkl` · `market_gateway` 响应字段早已存在但无测试钉住、UI 从未渲染；补齐网关钉住测试与 `ParticipationCard` 版本串渲染（含改前失败的 UI 测试），`bash scripts/test_changed.sh` 全绿。
- 2026-08-26 · 评估零实现能力（期权异动/社媒情绪/全市场扫描）最小竖切片 · `claude/token-quota-check-4lvpkl` · 排查确认三项各有明确插入点、不需要新架构；拆成三条独立 P2 竖切片插入队列（均为"打通道不做算法"，延续 PIT 校验/显式报错红线），未改代码，本轮无需跑 `test_changed.sh`（docs-only diff 会被其判定为"没有检测到改动"）。
