[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "us-stock-helper-analysis-api"
version = "0.1.0"
description = "Read-only HTTP boundary for the point-in-time decision chain"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["us_stock_helper_analysis_api*"]
