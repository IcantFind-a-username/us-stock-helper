[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "us-stock-helper-adviser-layer"
version = "0.1.0"
description = "Evidence-gated bounded adviser council"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["adviser_layer*"]

[tool.setuptools.package-data]
adviser_layer = ["py.typed"]
